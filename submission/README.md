# Moved to `docs/`

The hackathon submission page is now at **`../docs/index.html`**.

GitHub Pages only allows **`/docs`** or **`/` (root)** as the publish folder — not `/submission`.

**Enable Pages:** Settings → Pages → Branch `main` → Folder **`/docs`**
